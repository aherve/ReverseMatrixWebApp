div.big-padding
  md-subheader.md-accent Définissez vos critères de recherche

  md-content.md-padding

    md-input-container
      label Nom du projet
      input(
        ng-model="project.name"
        type="text"
        name="name"
      )

    md-input-container
      label Ville de référence
      input(
        ng-model="project.town"
        type="text"
        name="name"
      )

    md-input-container
      label Distance max : {{ project.max_distance }}
    md-slider( flex, min="0", max="100", ng-model="project.max_distance", class="md-accent")

    md-input-container
      label Surface min : {{ project.min_surface }}
    md-slider( flex, min="0", max="100", ng-model="project.min_surface", class="md-accent")

    md-input-container
      label Surface min : {{ project.max_surface }}
    md-slider( flex, min="0", max="100", ng-model="project.max_surface", class="md-accent")

    div( layout, layout-align="end" )
      md-button.md-raised.md-warn( 
        ng-click="createProject()"
      )
        | Créer le projet


